# CLAUDE.md — apps/ai

`@bajigur/ai` is a **placeholder**. It contains a `package.json`, a `README.md`,
and this file, and nothing else.

Do not scaffold it, add dependencies, or write code here unless a task asks for
it explicitly. When it is picked up, follow the workspace rules in
`../CLAUDE.md`: expose `dev`, `build`, `test`, `typecheck`, and `clean` scripts,
extend `@bajigur/tsconfig`, and put anything shared in `@bajigur/core`.

See `README.md` for the intended purpose.

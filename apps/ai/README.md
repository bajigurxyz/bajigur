# @bajigur/ai

**Placeholder — nothing is implemented yet.**

Reserved for the AI side of the platform: prompt generation, prompt evaluation,
and any agent that consumes the x402-gated API in `apps/api` and pays for calls
on its own budget.

The Hedera "AI & Agentic Payments" track needs an agent that discovers an
x402-gated service and completes a real paid request end to end without an API
key or a subscription. That agent is expected to live here.

Nothing here is decided: runtime, model provider, and framework are all open.

Owner: unassigned.

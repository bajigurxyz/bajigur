# @bajigur/landingpage

The public marketing site for Bajigur. Static and read-only — everything
interactive lives in `@bajigur/web`.

Next.js 16 (App Router) · React 19 · Tailwind CSS v4 · React Compiler.

```bash
bun run dev --filter=@bajigur/landingpage     # http://localhost:3001
```

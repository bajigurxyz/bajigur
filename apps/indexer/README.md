# @bajigur/indexer

**Placeholder — nothing is implemented yet.**

Reserved for the onchain indexer: reading contract events from `contracts/` and
serving them to `apps/api` and `apps/web` in a query-friendly shape.

Likely inputs are Hedera testnet events (payment settlements, access grants) and
whatever the payment contracts end up emitting. The indexing technology has not
been chosen.

Owner: unassigned.

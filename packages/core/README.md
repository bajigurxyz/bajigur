# @bajigur/core

Types and utilities shared across `apps/*`.

```ts
import { APP_NAME, isAddress, type Address } from "@bajigur/core";
```

Imported directly from source — no build step. Keep it free of runtime-specific
APIs so it works under Bun and in the browser.

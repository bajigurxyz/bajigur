# Claude Agent Skills

**Placeholder — no skills are written yet.**

This directory will hold the Claude Agent Skills that teach an agent when, why,
and how to use the Bajigur prompt library: discovering a prompt, paying for it
over x402, and applying the result.

Each skill is its own directory containing a `SKILL.md` with YAML frontmatter:

```
skills/
  <skill-name>/
    SKILL.md
```

Skills pair with the MCP server in `../apps/mcp`, and with the Recipes published
on bazantic.com for the Bazantic track.

Owner: unassigned.
